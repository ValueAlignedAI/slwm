[project]
name = "slwm-i1-baselines"
version = "0.0.0"
description = "Sprint I1 baseline models and smoke-training utilities for SLWM-124M."
requires-python = ">=3.10"
dependencies = ["numpy>=2.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
